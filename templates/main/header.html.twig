<header class="header">

	<a class="title" href="{{ path('homepage') }}">Topic Chat</a>

	{% if app.user %}
		<a class="add-btn" href="{{ path('app_logout') }}">Add Room</a>
		<a class="logout-btn" href="{{ path('app_logout') }}">Logout</a>
		
	{% endif %}
    
</header>
